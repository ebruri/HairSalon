@{
  Layout = "_Layout";
}

@model HairSalon.Models.Client

<h2>Are you sure you want to delete this client?</h2>

@Html.DisplayNameFor(model => model.ClientName): @Html.DisplayFor(model => model.ClientName)

@using (Html.BeginForm())
{
  <button type="submit" value="delete" class="btn btn-primary" style="width:250px">Delete Client</button>
}
@Html.ActionLink("Back to List", "Index")